/*
Expression:
a piece of code that produces a value. An expression can be a literal, a variable, a function call, or any combination of these. For example, `5`, `x`, and `add(2, 3)` are all expressions.
*/
// examples of expressions
let x = 5;
let y = 200;
let z = x + y;
console.log(z);

// examples of statements
let a = 10;
if(a > 5){
    console.log('a is greater than 5');
}